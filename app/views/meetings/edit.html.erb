<div class="ui raised very padded text container segment">
    <%= link_to "Atras", :back %>
    <h2 class="ui header">Editar reunión</h2>
    <div class="ui form">
        <%= form_for(@meeting) do |f| %>

        <%= f.hidden_field :id %>

        <div class="field">
            <%= f.label :descripcion %><br />
            <%= f.text_area :descripcion%>
        </div>

        <div class="field">
            <%= f.label :activity %><br />
            <%= f.select :activity_id, options_from_collection_for_select(Activity.all, :id, :tipo, @meeting.activity_id) %>
        </div>

        <div class="actions">
            <%= f.submit "Guardar", class: "ui primary button" %>
        </div>

        <% end %>
    </div>
</div>

<%= javascript_include_tag "validate/meeting" %>
