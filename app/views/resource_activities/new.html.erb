<div class="ui raised very padded text container segment">
    <%= link_to "Atras", :back %>
    <h2 class="ui header">Nuevo Recurso</h2>
    <div class="ui form">
        <%= form_for(@resource_activity) do |f| %>

        <div class="field">
            <%= f.label :actividad %><br />
            <%= f.select :activity_id, options_from_collection_for_select(Activity.all, :id, :descripcion),
            :include_blank => 'Seleccionar Actividad' %>
        </div>

        <div class="field">
            <%= f.label :descripción %><br />
            <%= f.text_area :descripcion %>
        </div>

        <div class="field">
            <%= f.label :precio %><br />
            <%= f.number_field :precio, min: 0.01, step: 0.01, autofocus: true %>
        </div>


        <div class="field">
            <%= f.label :cantidad %><br />
            <%= f.number_field :cantidad %>
        </div>

        


        <div class="actions">
            <%= f.submit "Guardar", class: "ui primary button" %>
        </div>

        <% end %>
    </div>
</div>